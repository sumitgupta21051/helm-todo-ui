apiVersion: v1
kind: Service
metadata:
  name:  {{ .Release.Name }}-delete-task
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-delete-task
  ports:
  - port: {{ .Values.deltask.serviceport }}
    targetPort: {{ .Values.deltask.containerport }}